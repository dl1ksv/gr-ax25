id: afsk_aprs2inet
label: Decode aprs data
category: '[Custom]/AX.25'

parameters:
 
-   id: sample_rate
    label: Sample rate
    dtype: int
    default: 11025

-   id: debug_level
    label: Debug Level
    dtype: int
    default: 4

inputs:

-   domain: stream
    label: in
    dtype: float

outputs:

-   domain: stream
    label: out
    dtype: byte

templates:

    imports: import afsk
    make: "afsk.aprs2inet(${sample_rate}, ${debug_level})"


file_format: 1    
